// Example...
package main

import (
    "fmt"
    "github.com/koden-km/math-go/quat"
)

func main() {
	var qa := quat.Zero()				// Make a new Quat that is all zero
	var qb := quat.New(w, x, y, z)		// Make a new Quat that has w,x,y,z set to the params.
}
